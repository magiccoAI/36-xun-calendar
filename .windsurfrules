# 前端设计与 UI 重构准则

当你进行 UI 开发或重构时，必须遵循以下原则：

## 1. 设计系统优先
- 使用 Tailwind CSS 进行样式开发，避免硬编码颜色值，优先使用主题变量。
- 组件库建议：优先使用 Radix UI 或 Shadcn UI 等无样式/可访问性良好的组件。
- 图标库：统一使用 Lucide React。

## 2. 视觉层级与布局
- 确保清晰的视觉锚点，重要信息通过字重、颜色或大小进行区分。
- 遵循响应式设计原则，优先考虑移动端体验（Mobile First）。
- 间距（Spacing）必须遵循 4px 或 8px 步进系统。

## 3. 交互反馈
- 所有交互动作（按钮点击、加载、悬停）必须有明确的视觉反馈。
- 状态处理：必须设计 Empty States、Loading States 和 Error States。

## 4. 辅助功能 (Accessibility)
- 确保语义化 HTML 标签的使用。
- 颜色对比度必须符合 WCAG AA 标准。
- 所有交互元素必须支持键盘导航。